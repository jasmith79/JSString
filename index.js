const {
  isNumeric,
  capFirst, 
  capFirstAll, 
  toCamelCase, 
  toSnakeCase, 
  toClassCase, 
  toPascalCase,
  padLeft,
  padRight,
  sprintf,
  random,
} = require('./dist/jsstring.js');

module.exports = {
  isNumeric,
  capFirst, 
  capFirstAll, 
  toCamelCase, 
  toSnakeCase, 
  toClassCase, 
  toPascalCase,
  padLeft,
  padRight,
  sprintf,
  random,
};
